# LR3 — Docker: сети, volume и docker compose

**Студент:** Вардания Леван Меджитович  
**Группа:** K0409-24-2

## Цель работы

Изучить работу Docker-сетей, persistent volume и запуск нескольких сервисов через docker compose.

## Ход работы

### 1. Проверка Docker-сетей

Сначала были просмотрены доступные сети Docker:

```bash
docker network ls
```

После этого была создана отдельная сеть app-network, в которой был запущен контейнер PostgreSQL.
Из временного контейнера Alpine была проверена доступность базы данных по имени контейнера.

Проверка ping:

Проверка порта 5432:

2. Проверка volume и сохранности данных

Был создан volume pgdata, затем PostgreSQL был запущен с подключённым volume.
В базе была создана таблица items и добавлены тестовые данные.

После удаления контейнера и запуска нового контейнера с тем же volume данные сохранились.

Результат проверки:

Информация о volume:
docker volume inspect pgdata

3. Работа с docker compose

Далее был создан compose-проект из трёх сервисов:

db — PostgreSQL
backend — Flask/Gunicorn
frontend — nginx

Сначала возникла проблема: в системе не был установлен docker compose, поэтому его пришлось доустановить.

Также была проблема с frontend, потому что контейнер не проходил healthcheck и оставался в состоянии starting/unhealthy. После исправления healthcheck сервис начал определяться корректно.

После запуска compose-проекта все сервисы перешли в состояние healthy:

docker compose ps

4. Проверка API через nginx

Далее был выполнен запрос к API через nginx:

В ответ были получены данные из базы данных:

5. Масштабирование backend

После этого backend был масштабирован до трёх экземпляров:

docker compose up -d --scale backend=3
docker compose ps

На скриншоте видно 3 экземпляра backend:

В ходе работы были изучены Docker-сети, volume и docker compose.